;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;;;             MAIN CONFIG EMACS             ;;;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; windows config
(require 'my-windows)

;; org config
(require 'my-org)

;; wanderlust config
(require 'my-wl)

;; gnus config
(require 'my-gnus)

;; irc config
(require 'my-irc)

;; pidgin config
(require 'my-pidgin)

;; latex config
(require 'my-latex)

;; programming config
(require 'my-programming)

;; shell config
(require 'my-shell)

;; github config
(require 'my-github)

;; internet config
(require 'my-internet)

;; rss config
(require 'my-rss)

;; multimedia config
(require 'my-multimedia)

;; ;; intel config
;; (require 'jmasson-intel)


(provide 'my-config)
