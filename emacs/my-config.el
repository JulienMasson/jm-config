;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;;;             MAIN CONFIG EMACS             ;;;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; windows config
(require 'my-windows)

;; org config
(require 'my-org)

;; irc config
(require 'my-irc)

;; programming config
(require 'my-programming)

;; shell config
(require 'my-shell)

;; github config
(require 'my-github)

;; rss config
(require 'my-rss)

;; work config
;; (require 'my-work)

;; home config
(require 'my-home)

;; keybindings config
(require 'my-keybindings)


(provide 'my-config)
